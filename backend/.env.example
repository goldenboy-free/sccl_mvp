# Server port (Render/Railway will set this automatically)
PORT=3000

# JWT secret key (generate a strong random string for production)
JWT_SECRET=your_jwt_secret_here

# CORS allowed origins (comma-separated, set to your Vercel URL in production)
CORS_ORIGIN=http://localhost:5173

# Node environment
NODE_ENV=development
